# dt-refactored-fiesta
A plugin for datatable which enables to edit data directly from table (Open modal if user requires). Primary objective is add calendar, conditional dropdown(using Ajax) to table cell.
